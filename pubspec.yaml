name: "transparent_image"
description: "A simple transparent 1x1 pixel image, implemented as an ImageProvider."
version: "3.0.0"
repository: "https://github.com/brianegan/transparent_image"
issue_tracker: "https://github.com/brianegan/transparent_image/issues"

environment:
  sdk: "^3.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: "flutter"

dev_dependencies:
  flutter_test:
    sdk: "flutter"
